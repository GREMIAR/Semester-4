using System.Windows.Forms;

namespace CommisVoyageur
{
    public partial class MainForm : Form
    {
        public bool FindKP(int indexFirst,int indexSecond)
        {
            int Length=0;
            foreach (Path path in points[indexFirst].paths)
            {
                
            }
            return true;
        }
    }
}
